# smoke.env.example - Copy this file to smoke.env and fill in your token.
# smoke.env is gitignored and must never be committed.
#
# Generate a token at https://github.com/settings/tokens?type=beta
# Required permission: Copilot > Requests > Read-only
GITHUB_TOKEN=
