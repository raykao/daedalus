apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "daedalus.fullname" . }}-registry
  labels:
    {{- include "daedalus.labels" . | nindent 4 }}
data:
  agent-registry.json: |
    {{- .Values.registry.config | nindent 4 }}
