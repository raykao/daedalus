apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-forge.fullname" . }}-registry
  labels:
    {{- include "agent-forge.labels" . | nindent 4 }}
data:
  agent-registry.json: |
    {{- .Values.registry.config | nindent 4 }}
