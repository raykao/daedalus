# This rule is not used by the project agent-forge-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to forge.agentforge.dev resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: agent-forge-operator
    app.kubernetes.io/managed-by: kustomize
  name: agentcard-viewer-role
rules:
- apiGroups:
  - forge.agentforge.dev
  resources:
  - agentcards
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - forge.agentforge.dev
  resources:
  - agentcards/status
  verbs:
  - get
