---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: modelconfigs.daedalus.dev
spec:
  group: daedalus.dev
  names:
    kind: ModelConfig
    listKind: ModelConfigList
    plural: modelconfigs
    singular: modelconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ModelConfig is the Schema for the modelconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ModelConfig
            properties:
              allowedNamespaces:
                description: allowedNamespaces defines which namespaces may reference
                  this ModelConfig.
                properties:
                  from:
                    default: Same
                    description: from specifies the namespace scope.
                    enum:
                    - All
                    - Same
                    - Selector
                    type: string
                  selector:
                    description: selector is a label selector for namespaces. Only
                      used when from=Selector.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - from
                type: object
              apiEndpoint:
                description: apiEndpoint is the provider API base URL. Omit for default
                  provider endpoints.
                type: string
              apiKey:
                description: apiKey provides the API key via inline value or secret
                  reference.
                properties:
                  value:
                    description: value is an inline string value. Mutually exclusive
                      with valueFrom.
                    type: string
                  valueFrom:
                    description: valueFrom specifies a source for the value. Mutually
                      exclusive with value.
                    properties:
                      configMapKeyRef:
                        description: configMapKeyRef selects a key from a ConfigMap.
                        properties:
                          key:
                            description: key is the key within the ConfigMap or Secret.
                            type: string
                          name:
                            description: name is the name of the ConfigMap or Secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      secretKeyRef:
                        description: secretKeyRef selects a key from a Secret.
                        properties:
                          key:
                            description: key is the key within the ConfigMap or Secret.
                            type: string
                          name:
                            description: name is the name of the ConfigMap or Secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                type: object
              headers:
                description: headers are additional HTTP headers sent with API requests.
                items:
                  description: |-
                    ValueRef provides a flexible way to specify configuration values.
                    Values can be inline, from a ConfigMap, or from a Secret.
                  properties:
                    name:
                      description: name is the key name for this value (e.g., "api-key",
                        "model-name").
                      type: string
                    value:
                      description: value is an inline string value. Mutually exclusive
                        with valueFrom.
                      type: string
                    valueFrom:
                      description: valueFrom specifies a source for the value. Mutually
                        exclusive with value.
                      properties:
                        configMapKeyRef:
                          description: configMapKeyRef selects a key from a ConfigMap.
                          properties:
                            key:
                              description: key is the key within the ConfigMap or
                                Secret.
                              type: string
                            name:
                              description: name is the name of the ConfigMap or Secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        secretKeyRef:
                          description: secretKeyRef selects a key from a Secret.
                          properties:
                            key:
                              description: key is the key within the ConfigMap or
                                Secret.
                              type: string
                            name:
                              description: name is the name of the ConfigMap or Secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                  required:
                  - name
                  type: object
                type: array
              model:
                description: model is the model identifier (e.g., "gpt-4o", "claude-sonnet-4-20250514",
                  "copilot").
                type: string
              parameters:
                additionalProperties:
                  type: string
                description: parameters are model-specific parameters (temperature,
                  max_tokens, etc.).
                type: object
              provider:
                description: provider identifies the LLM provider (e.g., "github-copilot",
                  "openai", "anthropic", "bedrock").
                type: string
            required:
            - model
            - provider
            type: object
          status:
            description: status defines the observed state of ModelConfig
            properties:
              conditions:
                description: conditions represent the current state of the ModelConfig
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              secretHash:
                description: |-
                  secretHash is a SHA-256 hash of the referenced secret data.
                  The controller reconciles downstream AgentRuntimes when this hash changes,
                  enabling automatic credential rotation propagation.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
