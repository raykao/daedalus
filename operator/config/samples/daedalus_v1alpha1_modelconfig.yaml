apiVersion: daedalus.dev/v1alpha1
kind: ModelConfig
metadata:
  name: github-copilot
  namespace: daedalus
spec:
  provider: github-copilot
  model: copilot
  apiKey:
    valueFrom:
      secretKeyRef:
        name: github-credentials
        key: token
  allowedNamespaces:
    from: Same
